const payment=(principal,annual,months)=>{const r=annual/1200;return r===0?principal/months:principal*r*Math.pow(1+r,months)/(Math.pow(1+r,months)-1)};
const future=(initial,monthly,annual,years)=>{const months=Math.round(years*12),r=annual/1200;let total=initial;for(let i=0;i<months;i++)total=total*(1+r)+monthly;return total};
const close=(actual,expected,tolerance=.02)=>Math.abs(actual-expected)<=tolerance;
if(!close(payment(120000,0,24),5000))throw new Error('Zero-rate loan payment failed');
if(!close(payment(150000,18,36),5422.86,.1))throw new Error('Loan payment formula failed');
if(!close(future(10000,0,0,5),10000))throw new Error('Zero-rate savings formula failed');
if(future(10000,2000,6,5)<=130000)throw new Error('Savings projection failed');
const salaryA=18000-2500,salaryB=22000-3500;
if(salaryB-salaryA!==3000)throw new Error('Salary comparison failed');
console.log('Simulator test passed: salary, loan and savings comparisons.');
