# V5.6.12 — JS Hotfix

- Se reemplazaron referencias a archivos JavaScript `.min.js` por sus versiones estables `.js`.
- Corrige los 404 detectados por Semrush en `catalog.min.js` y `common.min.js` y previene el mismo problema en calculadoras y módulos especializados.
- Se actualizó el Service Worker a `calculadoras-mx-v5.6.12` para invalidar la caché anterior.
- Se conservan las mejoras SEO, CSS y enlazado interno de V5.6.11.
