# Calculadoras México V5.6.25 — Fix global de enlaces 404

- Se normalizaron los enlaces internos de todas las páginas HTML a rutas absolutas desde la raíz (`/...`).
- Se revisó especialmente el hub `/articulos.html` y todos los artículos bajo `/articulos/`.
- Se validó que cada enlace interno HTML apunte a un archivo existente dentro del proyecto.
- Se validó que todas las URLs del sitemap correspondan a archivos existentes.
- Se eliminó documentación histórica del ZIP; solo se conserva este README esencial.
